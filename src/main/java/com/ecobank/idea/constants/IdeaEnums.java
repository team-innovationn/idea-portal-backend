package com.ecobank.idea.constants;

public class IdeaEnums {
    public enum Status {
        PENDING,
        APPROVED,
        REJECTED
    }

    public enum Submission {
        GROUP,
        INDIVIDUAL
    }
}